<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <a class="nav-link active text-black-50 text-decoration-none" aria-current="page" href="#">Wikinimous</a>
    <form class="d-flex">
      <a class="nav-link active text-black-50 text-decoration-none" aria-current="page" href="#">New Article</a>
    </form>
  </div>
</nav>

<div class="container mx-auto">
  <% @articles.each do |article| %>
      <h2> <%= link_to article.title, article_path(article) %> </h2>
      <p> <%= article.content %> </p>
  <% end %>
</div>
